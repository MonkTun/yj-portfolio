"use client";

import type { BitsBackgroundProps } from "./_types";
import { BitsPlaceholder } from "./_placeholder";

export default function GridScan(_: BitsBackgroundProps) {
  return (
    <BitsPlaceholder
      label="Grid Scan"
      url="https://reactbits.dev/backgrounds/grid-scan"
    />
  );
}
