"use client";

import type { BitsBackgroundProps } from "./_types";
import { BitsPlaceholder } from "./_placeholder";

export default function LiquidEther(_: BitsBackgroundProps) {
  return (
    <BitsPlaceholder
      label="Liquid Ether"
      url="https://reactbits.dev/backgrounds/liquid-ether"
    />
  );
}
